# Empty = same origin. Vite proxies /api to http://127.0.0.1:3000 in `npm run dev`.
# Required so a Windows browser can talk to the API running in WSL.
# For production, set the public API origin, e.g. https://api.example.com
VITE_API_URL=
